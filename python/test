#!/usr/bin/bash

if PYTHONPATH=. pytest --cov-config=.coveragerc --cov-report term-missing --cov=main tests/; then
    git add .
    git commit -m "autocommit: passing all tests with >= 90% coverage"
fi
